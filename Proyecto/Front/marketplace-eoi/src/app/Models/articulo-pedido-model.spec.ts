import { ArticuloPedidoModel } from './articulo-pedido-model';

describe('ArticuloPedidoModel', () => {
  it('should create an instance', () => {
    expect(new ArticuloPedidoModel()).toBeTruthy();
  });
});
